## Subtask slugify-sh

- **minor** nimbalyst-local/tools/slugify.sh:7 (die() defined) vs entire argument-handling path lines 50-66: die() is defined matching agent-context.sh:8 style but is never called anywhere in the script. Repro: `./slugify.sh --help` or `./slugify.sh --bogus-flag` — both silently slugify the flag text ('help', 'bogus-flag') and exit 0 instead of calling die() with a usage error. There is no argument-validation path that ever invokes die(), so it is dead code. If the subtask intent was 'die() helper used for error handling matching the local convention' (as agent-context.sh uses it for real validation failures), this diff only satisfies the letter (helper exists, matches printf/exit-1 shape) not the spirit (no caller, no error path exercised, no self-check case for it).
  needed tests: Either: (a) confirm with the task owner that no error path is required for this tool (single-purpose stdin/argv joiner with no validation surface) and delete die() as unused, or (b) add explicit rejection of unrecognized leading-dash flags (excluding literal args after `--`) via die(), plus a self-check case asserting non-zero exit and stderr message for an unknown flag.
- **minor** nimbalyst-local/tools/slugify.sh (no --help/-h handling): `./slugify.sh --help` produces `help` on stdout and exit 0 rather than printing usage. Not explicitly required by the subtask description, but is a common footgun: a caller probing the tool with --help silently gets slugified garbage instead of a usage message or error.
  needed tests: Not required by spec as written; no test needed unless the tool is expected to expose --help. Flagging only as a documentation/behavior gap, not a spec violation.
- **minor** nimbalyst-local/tools/slugify.sh:9-12 (slugify function) — self-check coverage vs subtask's explicit list: Subtask enumerates 8 required self-check categories: normal text, whitespace-only, leading/trailing punctuation, consecutive punctuation, non-ASCII byte input, empty stdin, multi-arg join, and `--` literal-string handling (including literal '--self-check' after --). All 8 are present and pass (verified by running `./slugify.sh --self-check`, exit 0, 8/8 PASS lines). No test-gap found against the literal enumerated list.
  needed tests: None required — coverage matches the spec's explicit list. Optional extra hardening (not required): a case for stdin with no trailing newline (verified manually to work correctly: `printf 'noeof' | slugify.sh` → `noeof`), and a case for input that is only punctuation/whitespace producing the same empty-line result as whitespace-only (verified manually: `printf '!!!...???' | slugify.sh` → empty line, exit 0).
