---
name: archon-adversarial-dev
description: Run the project's adversarial development workflow for scoped implementation work requiring independent critique, validation, and review.
---

# archon-adversarial-dev

This is the Codex entrypoint for the project skill. Its canonical instructions and
supporting workflow are maintained at `.claude/skills/archon-adversarial-dev/` so
Claude and Codex cannot drift.

Before using this skill, resolve the repository root with `git rev-parse --show-toplevel`
and read `<root>/.claude/skills/archon-adversarial-dev/SKILL.md` in full. Apply those
instructions, adapting only unavailable tool names to the equivalent Codex capability.
If the canonical file is absent, report that compatibility source as missing.
