---
name: research-team
description: Perform citation-backed, independently fact-checked research on a codebase, external topic, or both; state uncertainty rather than guessing.
---

# research-team

This is the Codex entrypoint for the project skill. Its canonical instructions and
supporting workflow are maintained at `.claude/skills/research-team/` so Claude and
Codex cannot drift.

Before using this skill, resolve the repository root with `git rev-parse --show-toplevel`
and read `<root>/.claude/skills/research-team/SKILL.md` in full. Apply those
instructions, adapting only unavailable tool names to the equivalent Codex capability.
If the canonical file is absent, report that compatibility source as missing.
